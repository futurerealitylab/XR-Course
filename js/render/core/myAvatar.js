export const selfDefinedAvatar = (head, leftHand, rightHand) => {
    let avatar = {head: null, leftHand: null, rightHand: null};
    // avatar["head"] = head.add("sphere").scale(0.1).color(0,0,0);
    return avatar;
}